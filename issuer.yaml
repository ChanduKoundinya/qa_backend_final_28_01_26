apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    # 1. Provide a valid email for expiration notices
    email: kazachandu.koundinya@neurealm.com 
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - http01:
        ingress:
          # 2. Ensure this matches your Ingress Class exactly
          class: nginx